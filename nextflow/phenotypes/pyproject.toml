[project]
name = "phenotypes"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
    { name = "Aine Fairbrother-Browne", email = "fairbrot@ebi.ac.uk" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
phenotypes = "phenotypes:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
