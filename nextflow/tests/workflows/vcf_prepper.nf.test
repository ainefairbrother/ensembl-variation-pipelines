nextflow_pipeline {

    name "Test Workflow vcf_prepper.nf"
    script "workflows/vcf_prepper.nf"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example: 
                // outdir = "tests/results"
            }
        }

        then {
            assert workflow.success
        }

    }

}
